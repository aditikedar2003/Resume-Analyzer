# Copy this to .env and fill values
# DATABASE: if empty, app uses local sqlite fallback (good for quick local testing)
DATABASE_URL=postgresql://resume_user:password@host:5432/resume_analyzer_xv7a

# SQLite path fallback (used if DATABASE_URL is empty)
SQLITE_PATH=./data/resume_analyzer.db

# Session secret
SESSION_SECRET_KEY=replace_with_random_secret
